# --- Dependencias y build ---
node_modules/
dist/
build/
.vite/
.turbo/
.cache/

# --- Variables de entorno y secretos ---
.env
.env.*
!.env.example
.envrc
.env.local
.env.*.local
*.local.env

# Archivos de credenciales (nombres habituales)
secrets.json
secrets.yaml
secrets.yml
credentials.json
credentials.yaml
credentials.yml
*-credentials.json
*-credentials.yaml
service-account*.json
firebase-adminsdk*.json
.supabase/

# Claves, certificados y material criptográfico
*.pem
*.key
*.p12
*.pfx
*.crt
*.cer
*.der
*.jks
*.keystore
id_rsa
id_rsa.*
id_ed25519
id_ed25519.*
*.secret
*.secrets

# Tokens / auth local
.netrc
.npmrc.local
*.auth-state.json
*.session
*.token

# --- Supabase CLI (estado local, no versionar) ---
supabase/.branches/
supabase/.temp/
supabase/.env
supabase/.env.*

# --- Despliegue (enlaces y env de Vercel/Netlify) ---
.vercel/
.netlify/

# --- Tests E2E / cobertura (pueden incluir cookies, capturas, dumps) ---
test-results/
playwright-report/
blob-report/
playwright/.cache/
coverage/
.nyc_output/

# --- Logs y diagnóstico ---
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# --- SO e IDE ---
.DS_Store
Thumbs.db
Desktop.ini
.idea/
*.swp
*.swo
*~

# Cursor / Claude: estado local del IDE
.cursor/*
!.cursor/rules/
!.cursor/rules/**
.claude/settings.local.json


# VS Code: solo overrides locales (settings compartidos sí pueden ir al repo)
.vscode/*.local.json
.vscode/settings.local.json

# --- Bases de datos / dumps locales ---
*.sqlite
*.sqlite3
*.db
*.dump
*.sql.gz
.vercel

# Capturas de la guía de usuario: son del CRM con datos reales de clientes
# (nombres, correos, teléfonos). Se regeneran con
# scripts/capture-guide-screenshots.mjs, así que no vale la pena meter 12 MB de
# PNG con datos personales en un historial que no se puede limpiar.
docs/user-guide-screenshots/
